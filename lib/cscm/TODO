* Subversion mimetypes (from the props)
* Changeset.added_nodes+friends returns object rather than arrays? 
* Always be outputting UTF-8
** Convert from other charsets if svn knows which one it is
* "binary node?" method

* Darcs adapter

* Memory profiling
* Come up with a better name than MockScm (AbstractScm?)